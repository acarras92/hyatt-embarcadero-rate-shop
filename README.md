# scaffolding/

Step 3 will populate this directory with verbatim copies of SHARED
files from the AKA repo (test suite, verification harnesses,
`scraper_lib` core modules: `normalize.py`, `validators.py`,
`schema.py`, `synxis_api.py`, `hotels_markdown.py`,
`hotels_vision.py`, `smoke_harness.py`, `audit_canonical_bar.py`,
`migrate_v1_to_v2.py`).

Also includes the verification harness scripts
(`scripts/phase2_anchor_validate.py`,
`scripts/phase2_high_bucket_audit.py`,
`scripts/phase5_plateau_sensitivity.py`,
`scripts/screenshot_dashboard.py`,
`scripts/merge_new_into_merged.py`) and the
`verification/apply_chrome_verification.py` ingest harness +
`verification/README.md` Cowork-side contract.

**Not yet populated.** Scaffold extraction is the next session's
work.
